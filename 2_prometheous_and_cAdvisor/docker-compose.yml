version: "3"

services:
  cadvisor:
    image: gcr.io/cadvisor/cadvisor:v0.47.1
    hostname: cadvisor
    platform: linux/aarch64
    volumes:
      - "/:/rootfs:ro"
      - "/var/run:/var/run:ro"
      - "/sys:/sys:ro"
      - "/var/lib/docker/:/var/lib/docker:ro"
      - "/dev/disk/:/dev/disk:ro"
    ports:
      - "8080:8080"

  prometheus:
    image: prom/prometheus
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
      - ./alert.rules.yml:/etc/prometheus/alert.rules.yml
    ports:
      - "9090:9090"

  grafana:
    image: grafana/grafana
    environment:
      GF_SECURITY_DISABLE_INITIAL_ADMIN_CREATION: "true"
      GF_AUTH_ANONYMOUS_ENABLED: "true"
      GF_AUTH_ANONYMOUS_ORG_ROLE: "Admin"
      GF_AUTH_DISABLE_SIGNOUT_MENU: "true"
      GF_AUTH_DISABLE_LOGIN_FORM: "true"
    volumes:
      - ./datasource.yaml:/etc/grafana/provisioning/datasources/datasource.yaml
    ports:
      - "3000:3000"

  loki:
    image: grafana/loki
    ports:
      - "3100:3100"
    volumes:
      - ./loki-config.yaml:/etc/loki/loki-config.yaml
    command: -config.file=/etc/loki/loki-config.yaml

  promtail:
    image: grafana/promtail
    volumes:
      # - /var/log:/var/log
      - ./promtail-config.yaml:/etc/promtail/promtail-config.yaml
      - /var/lib/docker/containers:/var/lib/docker/containers:ro
      - /var/run/docker.sock:/var/run/docker.sock
      - natures-cart_celery-logs:/app/logs/celery
      - natures-cart_cron-logs:/app/logs/cron

    command: -config.file=/etc/promtail/promtail-config.yaml

  # prometheus-msteams:
  #   image: faizan44/prometheus-msteams-arm64
  #   ports:
  #     - "2000:2000"
  #   environment:
  #     TEAMS_INCOMING_WEBHOOK_URL: "<--->" # Your actual webhook URL here
  #     TEAMS_REQUEST_URI: "alertmanager"
  #   #   CONFIG_FILE: "/tmp/config.yml"
  #   #   TEMPLATE_FILE: "/tmp/card.tmpl"
  #   # volumes:
  #   #   - /opt/prometheus-msteams/config.yml:/tmp/config.yml
  #   #   - /opt/prometheus-msteams/card.tmpl:/tmp/card.tmpl
  # depends_on:
  #   - prometheus

  alertmanager:
    image: prom/alertmanager:latest
    # container_name: alertmanager
    volumes:
      - ./alertmanager/alertmanager.yml:/etc/alertmanager/alertmanager.yml
    ports:
      - "9093:9093"

  prometheus-msteams:
    image: faizan44/prometheus-msteams-arm64
    # container_name: prometheus-msteams
    environment:
      TEAMS_INCOMING_WEBHOOK_URL: "https://alazkaai.webhook.office.com/webhookb2/9eaf5da9-fb45-4666-a36e-729e576178e0@7eaff8d0-7379-456d-8b49-b0ca02a107cb/IncomingWebhook/a7cf852772fe4d84af7837ff62e26137/958ff4fd-ee53-46ed-889b-6035009ba951/V2xxMwrMmGwB4dBi3NeCgFbY2CfKyykyMs5B42K7XCjXo1"
      TEAMS_REQUEST_URI: "alertmanager"
    ports:
      - "2000:2000"
    # volumes:
    #   - ./prometheus-msteams/default-message-card.tmpl:/default-message-card.tmpl
    # command: -server -template-file /default-message-card.tmpl
    depends_on:
      - prometheus

  # prometheus-msteams:
  #   image: faizan44/prometheus-msteams-arm64
  #   ports:
  #     - "2000:2000"
  #   environment:
  #     TEAMS_INCOMING_WEBHOOK_URL: "<--->" # Your actual webhook URL here
  #     TEAMS_REQUEST_URI: "alertmanager"
  #   #   CONFIG_FILE: "/tmp/config.yml"
  #   #   TEMPLATE_FILE: "/tmp/card.tmpl"
  #   # volumes:
  #   #   - /opt/prometheus-msteams/config.yml:/tmp/config.yml
  #   #   - /opt/prometheus-msteams/card.tmpl:/tmp/card.tmpl
  #   depends_on:
  #     - prometheus

volumes:
  natures-cart_celery-logs:
    external: true
  natures-cart_cron-logs:
    external: true
